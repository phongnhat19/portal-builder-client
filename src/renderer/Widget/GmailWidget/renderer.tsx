import React from 'react';

const Gmail = ({width, height, htmlString}: {
  htmlString?: string,
  width?: string | number,
  height?: string | number,
}) => {

  return (
    <div> Gmail </div>
  );
}

export default Gmail;